
F is followed by a number.

After each order press RETURN to start
 a new order.
Type RUN (and RETURN) when you want the
 arrow to start.

You may edit your program if it didn't
 work.  Select the line you want to
 change, then press delete and enter
 your new instruction (and then RETURN).

You get a higher score if you take a
 shorter route.

       PRESS ANY KEY TO CONTINUE